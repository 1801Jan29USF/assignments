package Week1HW.q10;

/**
 * Functionality: Implements the finding of a minimum of two integers using ternary operators.
 */
public class TernaryMinimum {
    public int minimum(int one, int two){
        return one < two ? one : two;
    } //returns one if true, two if false
}
